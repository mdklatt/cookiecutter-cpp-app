/**
 * Current version of the {{ cookiecutter.app_name }} application.
 *
 * This application uses the Semantic Versioning scheme:
 *
 *   <http://semver.org/>
 *
 * Major versions introduce significant changes to the API, and backwards
 * compatibility is not guaranteed. Minor versions are for new features and
 * other backwards-compatible changes to the API. Patch versions are for bug
 * fixes and internal code changes that do not affect the API.
 *
 * THIS FILE IS GENERATED BY CMAKE.
 *
 * @file
 */
#ifndef {{ cookiecutter.app_name | upper }}_VERSION_HPP
#define {{ cookiecutter.app_name | upper }}_VERSION_HPP

#include <string>


/**
 * Get the application version.
 *
 * @return version string
 */
inline std::string version() {
    static const std::string version{"@{{ cookiecutter.app_name }}_VERSION_MAJOR@.@{{ cookiecutter.app_name }}_VERSION_MINOR@.@{{ cookiecutter.app_name }}_VERSION_PATCH@"};
    static const std::string devel{"@{{ cookiecutter.app_name }}_VERSION_TWEAK@"};
    static const std::string suffix{devel.empty() ? "" : "." + devel};
	return version + suffix;
}


#endif  // {{ cookiecutter.app_name | upper }}_VERSION_HPP
